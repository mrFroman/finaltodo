#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../ts-migrate/bin/ts-migrate-full.sh" "$@"
  ret=$?
else 
  bash  "$basedir/../ts-migrate/bin/ts-migrate-full.sh" "$@"
  ret=$?
fi
exit $ret
